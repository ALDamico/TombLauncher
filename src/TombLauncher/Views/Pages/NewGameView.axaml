<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="using:TombLauncher.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:utils="clr-namespace:TombLauncher.Utils"
             xmlns:pages="clr-namespace:TombLauncher.ViewModels.Pages"
             xmlns:controls="clr-namespace:TombLauncher.Controls;assembly=TombLauncher.Controls"
             xmlns:iconPacks="https://github.com/MahApps/IconPacks.Avalonia"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="TombLauncher.Views.Pages.NewGameView"
             x:DataType="pages:NewGameViewModel">
    <UserControl.Styles>
        <Style Selector="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style Selector="TextBox">
            <Setter Property="Margin" Value="0, 0, 0, 15" />
        </Style>
    </UserControl.Styles>
    <Design.DataContext>
        <pages:NewGameViewModel />
    </Design.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="4"
                   HorizontalAlignment="Center"
                   Classes="h1"
                   Text="{loc:Translate INSERT_NEW_GAME}" />
        <DockPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" LastChildFill="True">
            <controls:ImagePicker DockPanel.Dock="Right"
                                  Source="{Binding GameMetadata.TitlePic, TargetNullValue={x:Static utils:ImageUtils.NullBitmap}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  DialogService="{Binding DialogService}"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  MaxHeight="240"
                                  MaxWidth="320" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Classes="small"
                               Text="{loc:Translate ZIP_ARCHIVE_OR_FOLDER}" />
                    <DockPanel Grid.Row="1" Grid.Column="0">
                        <controls:SplitIconButton Icon="FolderZipLine" Text="{loc:Translate OPEN_ZIP_FILE}"
                                                  VerticalAlignment="Top"
                                                  DockPanel.Dock="Right" Command="{Binding PickZipArchiveCmd}">
                            <controls:SplitIconButton.Flyout>
                                <MenuFlyout Placement="Bottom">
                                    <MenuItem Header="{loc:Translate OPEN_FOLDER}"
                                              Command="{Binding PickFolderCmd}">
                                        <MenuItem.Icon>
                                            <iconPacks:PackIconRemixIcon Kind="FolderLine" />
                                        </MenuItem.Icon>
                                    </MenuItem>
                                </MenuFlyout>
                            </controls:SplitIconButton.Flyout>
                        </controls:SplitIconButton>
                        <TextBox Text="{Binding Source}" />
                    </DockPanel>
                </Grid>

                <controls:LabeledTextBox Grid.Row="2"
                                         Grid.Column="0"
                                         Grid.ColumnSpan="2"
                                         Text="{Binding GameMetadata.Title}"
                                         Label="{loc:Translate TITLE}" />
                <controls:LabeledTextBox Grid.Row="3"
                                         Grid.Column="0"
                                         Text="{Binding GameMetadata.Author}"
                                         Label="{loc:Translate AUTHOR}" />
                <controls:LabeledCalendarDatePicker Grid.Row="3"
                                                    Grid.Column="1"
                                                    SelectedDate="{Binding GameMetadata.ReleaseDate}"
                                                    Label="{loc:Translate RELEASE_DATE}"
                                                    Margin="20, 0, 0, 0" />
                <Grid Grid.Row="4" Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0"
                               Classes="small"
                               Text="{loc:Translate LENGTH}" />
                    <ComboBox Grid.Row="1"
                              ItemsSource="{Binding AvailableLengths}"
                              SelectedValueBinding="{Binding Value}"
                              SelectedValue="{Binding GameMetadata.Length}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Description}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>
                <Grid Grid.Row="4" Grid.Column="1" Margin="20, 0, 0, 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Classes="small" Text="{loc:Translate DIFFICULTY}" />
                    <ComboBox Grid.Row="1"
                              ItemsSource="{Binding AvailableDifficulties}"
                              SelectedValueBinding="{Binding Value}"
                              SelectedValue="{Binding GameMetadata.Difficulty}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Description}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>
            </Grid>
        </DockPanel>


        <controls:LabeledTextBox Grid.Row="2"
                                 Grid.Column="0"
                                 Grid.ColumnSpan="4"
                                 Text="{Binding GameMetadata.Setting}"
                                 Label="{loc:Translate SETTING_MILIEU}" />

        <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Classes="small" Text="{loc:Translate SYNOPSIS}" />
            <TextBox Grid.Row="1" Text="{Binding GameMetadata.Description}"
                     AcceptsReturn="True" />
        </Grid>
        <controls:IconButton Grid.Row="4" Grid.Column="3" Icon="Save3Line" HorizontalAlignment="Right"
                             Text="{loc:Translate INSTALL}" Command="{Binding SaveCmd}" />
    </Grid>
</UserControl>