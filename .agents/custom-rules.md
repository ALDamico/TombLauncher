# Agent Rules
1. Never commit without explicit user consent. Do not run `git commit` or any auto-committing workflow step unless the user has explicitly authorized it for the current change.
2. Never perform `git stash` operations (push, pop, drop, apply, etc.) without explicit user request. Stash management is always the user's decision.
